# 네이버 부동산 서울 지역 데이터 크롤링 계획서

## 1. 개요
서울 지역의 부동산 매매/전세/월세 데이터를 수집하기 위한 크롤러 설계 문서입니다. 이 스크립트는 외부 배포 없이 개인 로컬 환경에서 시세 파악을 위해 직접 구동됩니다.

## 2. 수집 대상 데이터 항목
- **기본 정보**: 매물명, 법정동, 지번, 도로명 주소, 위도/경도 위치 좌표
- **가격 정보**: 매물 가격, 보증금/월세(해당 시), 관리비
- **상세 정보**: 평형(전용면적/공급면적), 층수, 방향, 방/욕실 수, 건축 연도, 세대수
- **기타**: 매물 특징 설명, 부동산 중개업소 정보

## 3. 수집 전략 (Sequential Approach)
1. **타겟 플랫폼 탐색**: 네이버 부동산 웹/앱 API의 Network 탭 통신 구조 분석
2. **수집 방식 확정**: 강화된 Anti-Bot 정책을 우회하기 위해 **Playwright**를 활용한 Network Intercept(네트워크 가로채기) 방식 채택. 브라우저로 화면을 뛰운 뒤, 백그라운드에서 교환되는 JSON 응답만 직접 탈취하여 파싱합니다.
3. **추출 범위 설정**: 서울특별시 전체 매물의 동/단지 코드 수집 목록 선행 구축
4. **업데이트 주기**: 필요시 로컬에서 파이썬 스크립트를 수동 또는 스케줄러로 실행하여 최신 데이터 직접 갱신

## 4. 리스크 관리 및 봇 탐지 방어 로직 (Anti-bot Evasion)
- **우회 도구 사용**: 일반 `requests` 모듈 대신 실제 크로미움(Chromium) 브라우저를 구동하는 Playwright를 사용하여 네이버의 쿠키 및 세션 검증 로직을 자연스럽게 통과합니다.
- **요청 속도 조절**: 랜덤 Delay 적용 및 사람과 유사한 패턴(User Agent 변조 등) 구사
- **데이터 정합성**: 네이버 부동산 매물 번호(식별자)를 기반으로 중복 수집 필터링
- **에러 핸들링**: 크롤링 실패 시 무한 루프 차단 및 일정 시간 후 재시도(Retry)

## 5. 저장 방식 (CSV Export)
- **CSV 파일 저장**: 시스템의 의존성을 줄이고 엑셀/판다스로 편하게 열람하기 위해, 정제된 데이터를 **`CSV 파일(.csv)`** 로 즉각 내보냅니다.
- **로컬 패스**: 수집된 데이터는 `data/csv_exports/{오늘날짜}_seoul_real_estate.csv` 와 같은 네이밍으로 저장되어, 즉시 로컬 프론트엔드 뷰어가 접근할 수 있도록 구성합니다.
